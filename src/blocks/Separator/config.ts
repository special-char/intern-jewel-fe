import { Block } from 'payload'

export const SeparateBlock: Block = {
  slug: 'separate',
  interfaceName: 'Separate',
  fields: [
    {
      name: "Separator",
      type: "group",
      fields: []
    }
  ]
}
