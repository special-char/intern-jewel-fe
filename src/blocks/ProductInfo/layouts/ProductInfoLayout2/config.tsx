import type { Block } from "payload"

export const ProductInfoLayout2: Block = {
    slug: "productinfolayout2",
    interfaceName: "ProductInfoLayout2",
    admin: { group: "Product Detail" },
    fields: [

    ],
}