import type { Block } from "payload"

export const ProductInfoLayout1: Block = {
    slug: "productinfolayout1",
    interfaceName: "ProductInfoLayout1",
    admin: { group: "Product Detail" },
    fields: [

    ],
}
