import type { Block } from "payload"

export const ProductInfoLayout3: Block = {
    slug: "productinfolayout3",
    interfaceName: "ProductInfoLayout3",
    admin: { group: "Product Detail" },
    fields: [

    ],
}