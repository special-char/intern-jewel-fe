import { Block } from 'payload'

export const SeparateBlock2: Block = {
  slug: 'separate2',
  interfaceName: 'Separate2',
  fields: [
    {
      name: "Separator2",
      type: "group",
      fields: []
    }
  ]
}
